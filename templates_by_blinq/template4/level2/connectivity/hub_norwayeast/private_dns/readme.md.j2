
```bash
#Note: close previous session if you logged with a different service principal using --impersonate-sp-from-keyvault-url
rover logout

# login a with a user member of the caf-maintainers group
rover login -t {{ ignite.global.onmicrosoft_domain }} \

rover \
{% if ignite.global.cred_keyvaults.cred_connectivity is defined and ignite.global.cred_keyvaults not like ignite.global.null %}
  --impersonate-sp-from-keyvault-url https://{{ ignite.global.cred_keyvaults.cred_connectivity }}.vault.azure.net \
{% endif %}
  -lz /tf/caf/landingzones/caf_solution \
  -var-folder {{ ignite.global.folders.configuration_folder }}/level2/connectivity/hub_norwayeast/private_dns \
  -tfstate_subscription_id {{ ignite.global.subscriptions.sub_management }} \
  -target_subscription {{ ignite.global.subscriptions.sub_connectivity }} \
  -tfstate connectivity_private_dns_zones.tfstate \
  -env {{ ignite.global.caf_environment }} \
  -level level2 \
  -w tfstate \
  -p ${TF_DATA_DIR}/connectivity_private_dns_zones.tfstate.tfplan \
  -a plan

```

